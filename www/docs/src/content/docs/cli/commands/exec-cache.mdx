---
title: nrz exec-cache
sidebar:
  label: exec-cache
---

import { Code } from '@astrojs/starlight/components'

Usage:

<Code
  code="$ nrz exec-cache <command> [flags]"
  title="Terminal"
  lang="bash"
/>

Work with nrz exec-cache folders

## Aliases

```
xc
```

## Subcommands

### ls

Show previously installed packages used for `nrz exec`. Key provided
can be either the package name, or the full key.

<Code code="$ nrz exec-cache ls" title="Terminal" lang="bash" />

### delete

Delete previously installed packages used for `nrz exec`. If no keys
are provided, then all entries will be deleted.

<Code
  code="$ nrz exec-cache delete [<key>...]"
  title="Terminal"
  lang="bash"
/>

### info

Show extended information about a given `nrz exec` installation.

<Code
  code="$ nrz exec-cache info <key>"
  title="Terminal"
  lang="bash"
/>

### install

Install the specified package(s) in the `nrz exec` central cache
location. Metadata info about each installation will be printed.

<Code
  code="$ nrz exec-cache install <spec>..."
  title="Terminal"
  lang="bash"
/>

## Examples

Show all the keys for the installations in the `nrz exec` cache:

<Code code="$ nrz exec-cache ls" title="Terminal" lang="bash" />

Delete all versions of typescript installed for nrz exec:

<Code
  code="$ nrz exec-cache delete typescript"
  title="Terminal"
  lang="bash"
/>

Show extended info about a specific version of typescript:

<Code
  code="$ nrz exec-cache info typescript-695bf962"
  title="Terminal"
  lang="bash"
/>
